name: Deploy Hospedagens → Hostinger (v2)

on:
  push:
    branches: [main]
    paths:
      - "Setor de Locação/gestao-hospedagens/**"
      - ".github/workflows/deploy-hospedagens.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 72.60.153.200
          username: u432659370.ojoaomendonca.com.br
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: "Setor de Locação/gestao-hospedagens/"
          server-dir: nodejs/public/hospedagens/
          exclude: |
            **/.git*
            **/apps-script.gs
